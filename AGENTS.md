# RMXP-Hero 网页移植：Agent 工作手册

本文件是本仓库的长期工程上下文。后续 Agent 在修改代码前必须先阅读本文件，并在实现新功能、修正原作逻辑或改变发布状态后同步更新相关章节。

## 1. 项目目标与当前状态

本项目将 `qq634488405/RMXP-Hero` 从 RPG Maker XP / RGSS Ruby 完整移植为浏览器可玩的单机游戏，同时满足：

- 原作玩法、数值、任务和战斗内核尽量逐公式保持一致。
- 全键盘操作，同时照顾 macOS 和 Windows 用户。
- 不直接发布原作位图；地图、场景、人物、建筑、物品和界面使用程序化像素绘制、Canvas 与 CSS 重新实现。
- 存档可导出为格式化 JSON，允许玩家查看和修改数值后重新导入。
- 通过 OpenAI Sites 构建和发布可直接游玩的生产版本。

当前状态：离线单人版主体移植已完成。根路由先显示原作风格标题页，可继续存档、开始新游戏、读取 JSON 或查看完整按键；新游戏依次经过原作序章和人物创建。正式版已完成桌面大屏、交互物和入口标记、非遮挡式视觉重构以及 SSR 安全的本地存档恢复。

- 首页：`https://rmxp-hero-web.weminl.chatgpt.site`
- 完整游戏：`https://rmxp-hero-web.weminl.chatgpt.site/original`
- Sites project ID：`appgprj_6a747f6fd3d08191a823e0e6b7f9fe42`
- 原作源码位于相邻工作区：`/Users/weiminglu/Documents/rmxp-new/Script`
- 网页项目根目录：`/Users/weiminglu/Documents/rmxp-new/web`

原作网络专用场景不属于离线浏览器产品范围。完整覆盖审计另见 `docs/PORT_AUDIT.md`，本轮逐项严格审查见 `docs/STRICT_PARITY_AUDIT.md`。

面向产品、玩法、美术和地图设计的当前完整说明见 `docs/CURRENT_GAME_DESIGN.md`。改变游戏系统、地图规划、人物美术映射、操作方式、测试基线或发布状态时，必须同步更新该文档。

## 2. 原作数据规模与提取结果

已提取并纳入浏览器运行时的数据包括：

- 69 张地图。
- 400 个地图事件。
- 955 条地图事件指令。
- 215 个 `Scene_Event` 调用。
- 60 条武功记录。
- 40 条主动绝招记录。
- 33 种物品。
- 32 种武器。
- 34 种防具。
- 199 个敌人或 NPC 记录。
- 108 个 Ruby 脚本文件。

提取后的数据位于 `game-data/`：

- `maps.json`：地图、图层、事件页与事件指令。
- `tilesets.json`：通行位和优先级。
- `enemies.json` / `enemies_plus.json`：NPC、敌人、商店、师父和战斗属性。
- `items.json`、`weapons.json`、`armors.json`：物品与装备。
- `kungfus.json`、`skills.json`：武功和绝招。
- `tasks.json`：任务配置。
- `core.json`、`config.json`：其他全局配置。
- `extraction-report.json`、`database-report.json`：提取覆盖报告。

重新提取数据时优先复用：

- `scripts/extract_rxdata.rb`
- `scripts/extract_gmud_data.rb`

不要手工改写大量生成数据来修逻辑；逻辑适配应放在 `app/game-core/`。

## 3. 核心目录与职责

### 页面和渲染

- `app/page.tsx`：正式游戏根路由，直接渲染 `OriginalWorld`。
- `app/hero-game.tsx`：早期简化体验实现，当前没有路由引用；不要重新接回正式入口。
- `app/original/page.tsx`：完整原版世界路由。
- `app/original/original-world.tsx`：完整游戏状态、键盘输入、地图循环、事件交互、战斗和所有模态 UI 的总编排。
- `app/original/world.css`：世界页布局。
- `app/original/battle.css`、`menu.css`、`choice.css`、`special.css`：各类游戏 UI。

### 数据与地图

- `app/game-core/original-data.ts`：原始数据库索引入口。
- `app/game-core/original-world.ts`：69 张地图加载、图块查询、通行判定、事件定位和转场。
- `app/game-core/rmxp-events.ts`：RMXP 指令适配和 `Scene_Event.new(...)` 解析。
- `app/game-core/scene-event.ts`：场景事件类型 0–16 的浏览器行为与人物状态类型。

### 游戏系统

- `npc-system.ts`：交谈、查看状态、切磋、交易、拜师、加入门派和请教。
- `npc-lore.ts`：全 NPC 世界观档案、身份性情、活动地点与对话系统提示词。
- `lm-studio.ts`：本地 LM Studio 的 OpenAI 兼容流式对话客户端。
- `inventory-system.ts`：背包、物品使用、战斗物品、装备槽和衍生属性。
- `skill-system.ts`：武功学习、运用、招架和基础功夫回退。
- `cultivation-system.ts`：打坐、冥思、吸气、疗伤、加力、法点和练功。
- `combat.ts`：RGSS 战斗中的基础命中、闪避、招架、伤害和整数运算。
- `original-battle.ts`：完整回合战斗、普通攻击、敌方行动、绝招、状态、逃跑和特殊战斗模式。
- `special-system.ts`：绝招解锁条件、消耗和冷却。
- `battle-settlement.ts`：金钱、战利品、杀人记录和道德结算。
- `task-system.ts`：主任务、悬赏、义工、石料、隐藏交换、坛任务和奖励。
- `survival-system.ts`：食物、饮水、恢复、消化和年龄。
- `life-system.ts`：铸剑、重铸、自制武器、房屋升级和家具。
- `cheat-system.ts`：完整存档修改器内核；快捷强化、受限数值、物品装备、全部武功、身份师承、复活 NPC 与世界进度。

## 4. 地图、事件与 NPC 交互

### 事件指令

地图实际使用的 RMXP 指令码都必须存在适配器。当前适配包括：

- `0`：结束。
- `201`：地图传送。
- `250`：音效。
- `355` / `655`：Ruby 脚本段，合并后解析 `Scene_Event.new(type, id, extra)`。

`selectSceneEvent` 支持原地图脚本中用到的条件子集，包括：

- 背包物品数量。
- `tan_id` 进度。
- `free_work` 状态。
- `can_get_item?`。
- `can_get_caihua?`。

### NPC 的特殊数据问题

原始 400 个地图事件中只有 3 个事件带 `character_name` 图形，但实际有 215 个脚本场景调用。大量 NPC 的地图事件只保存数值名字和 `Scene_Event.new(0, npcId)`，没有角色图片。

因此不能用 `page.graphic.character_name` 作为“是否为 NPC”的唯一依据。当前统一规则是：

1. 解析事件页脚本。
2. 如果场景类型为 `0` 且存在 `id`，该事件就是 NPC。
3. 使用 `npcRecord(id).name` 获取显示名。
4. 显示、碰撞和交互必须使用同一判定，不能各自实现不同规则。

当前可见性设计：

- 脚本型 NPC 会被程序化绘制成像素人物。
- 非玩家人物使用亮色描边。
- 脚下有金色交互环。
- 头顶有轻微跳动的感叹号。
- 玩家距离 NPC 两格以内时显示姓名。
- 通缉犯使用红色强调色。
- 非 NPC 场景事件会根据语义显示为青色可互动物。
- 地图传送及场景类型 13、15、16 显示为绿色门或入口，并始终显示入口名称。
- `EV010`、`EVENT12` 或纯数字等 RPG Maker 内部事件名不得直接显示；传送事件通过 `friendlyEventName` 回退到目标地图名称，例如红莲教 `EV010` 显示为“五指山”。
- 可互动物与 NPC 一样在靠近两格时显示名称。
- 发行地图中可执行的 215 个 `Scene_Event` 调用与全部传送事件都会显示为 NPC、交互物或入口；其余地图事件是纯装饰、空页或条件尚未满足的事件，不应伪装成当前可互动对象。

当前交互规则：

- NPC 是阻挡事件，玩家不能穿过。
- 按确认键时优先检查面前、当前格和四个相邻格的 NPC。
- 这样可以兼容旧存档中玩家与 NPC 已经重叠的情况。
- 如果附近没有 NPC，才触发面前一格的普通地图事件。
- NPC“查看”必须展示原作人物描述、年龄性别、气血/内力/法力、四维、战斗数据、经验/银两/道德、装备、携带物品、完整武功表、正在运用的功夫与商店货物；不能再用商店列表为空推断“身上没有可见物品”。
- NPC“查看”还要公开先天/实战四维、加力、法点、外貌、福缘与掉落概率。仅战斗 AI 的临时回合状态、随机种子和内部事件条件属于运行时内部数据，不作为人物固定属性展示。
- NPC 菜单的“自由对话”直接从浏览器流式访问本机 `http://127.0.0.1:1234/api/v1/chat`，默认模型为 `qwen3.6-35b-a3b-uncensored-hauhaucs-aggressive`；原作“交谈”继续承载任务推进，不得用自由对话替代。
- 全部 198 条有效 NPC/敌人记录（其中 182 人实际出现在地图事件中）都通过 `npc-lore.ts` 获得稳定档案；档案同时进入“查看”状态和 LLM system prompt。自由对话只在当前相遇的 React 状态中保留最近十条，不写 `localStorage` 或游戏存档；结束、移动一格或转场必须立即中止生成并清空。
- 自由对话 UI 是地图画布底部的非全屏临场浮层，玩家每轮可分别输入行动与语言，至少填写一项；NPC 回复必须解析为状态、动作、语言三个气泡内容。行动是叙事输入，不能未经游戏系统确认而直接改变物品、任务或战斗状态。
- 自由对话上半区固定显示可滚动历史，下半区固定显示行动、语言与控制栏。“自动对话”由模型分别扮演主角和 NPC，依据双方人设、关系、地图与世界资料轮流推进；结束对话、移动或转场必须中止当前请求和后续自动轮转。
- LLM 上下文必须使用双方可读信息而非裸 ID：NPC 身份、背景、年龄、外貌与综合武境；玩家门派、师父姓名、年龄、容貌、综合武境、兵器和名声道德。提示词要让 NPC 按师徒/同门关系、实力差距和正邪立场自然改变称呼与态度，但不能机械复述属性。
- 为降低本地模型首字延迟，请求必须保持 SSE 流式输出、短历史、有限输出长度，并关闭 Qwen thinking；不得在 Canvas 绘制循环中发请求。
- 本地 `localhost` 游戏页面通过同源 `/api/lm-studio` 路由转发请求，避免浏览器 CORS 预检；部署站点仍直接访问玩家电脑的 LM Studio，因此必须在 Developer → Server Settings 开启 Enable CORS。
- 原作 NPC 菜单第三项文字是“战斗”，调用 type 0 常规生死战，并非安全切磋；网页端必须进入 `lethal` 模式并在胜利后选择杀或放。

修改 NPC 绘制时必须同步验证：角色是否显示、是否阻挡、确认键是否能打开 NPC 菜单，以及转场事件是否仍能正常踩踏触发。

## 5. 键盘映射

必须同时支持字母键、方向键以及 Mac 键盘常见布局。浏览器默认滚动行为要在游戏使用的按键上阻止。

- 移动：`WASD` 或方向键。
- 确认 / 互动：`E` 或 `Enter`；不要绑定 `Z` 或空格。
- 返回 / 取消：`X` 或 `Escape`。
- 人物、背包和武功菜单：`C`。
- 战斗绝招：`Q`，同时保留 `C` 兼容入口。
- 战斗物品：`I`。
- 生死战逃跑：`G`；`X` 在生死战中也必须走逃跑判定，不能无条件退出。
- 保存进度：点击页面右上角“保存”按钮。
- 导出 / 导入 JSON：点击侧栏按钮。
- 轻功：`H`；有效轻功至少 30 级、位于室外且当前内力至少 200。
- 秘技：`K`。
- 不绑定字母 `F` 或任何 `F1–F12` 功能键，照顾 Mac 键盘布局。

所有选择菜单使用 `W/S` 或上下方向键移动，`E/Enter` 确认，`X/Escape` 返回。新增模态窗口必须接入键盘分支，不能只提供鼠标按钮。

## 6. 战斗模式与关键公式

`OriginalBattle.mode` 有三种：

- `spar`：NPC 菜单中的普通切磋。可以直接结束，不产生杀人、任务或掉落结算。
- `lethal`：地图敌人、悬赏犯和一般生死战。胜利后必须选择“砍头”或“手下留情”。
- `story`：不可逃跑的剧情战，例如铸剑挑战和桃花源山大王。

### 普通战斗

- 所有随机流程使用战斗种子派生的确定性随机数，测试依赖相同种子得到相同结果。
- RGSS 原作中的除法和属性运算应保持整数截断语义。
- 武器不匹配时回退到相应基础攻击技能。
- 装备的攻击功夫也可设为招架功夫。
- 绝招 1–40 的需求、消耗、冷却、法术链、反射和状态效果由原始数据及专门适配共同驱动。

### 逃跑

原作公式：

```text
escape_num = rand(actor.agi + escape_factor)
成功条件：escape_num >= enemy.agi
失败后：escape_factor += 10，并由敌人行动一次
```

- `story` 模式永远不能逃跑。
- 每次失败增加 10 点逃跑补偿。
- 失败不是免费操作，必须执行敌方回合。

### 战斗物品

- 战斗中允许 `occasion` 为 `0`（任何场景）或 `1`（仅战斗）的非秘籍物品。
- 菜单中允许 `occasion` 为 `0` 或 `2`。
- 战斗使用物品不额外模拟普通攻击回合；沿用原作 phase 1 行为。
- 消耗、百分比增益、气血、内力、食物和饮水都走原物品字段。

### 胜利、杀与放

一般生死战胜利后显示：

- `砍头`：记入杀人列表、影响道德并推进只认击杀的任务。
- `手下留情`：仍获得一般战利品和金钱，但不推进悬赏、杀人任务或坛击杀。

原作道德公式：

```text
若 actor.morals >= 128：actor.morals -= enemy.morals
否则若 enemy.morals > 0：actor.morals -= floor(enemy.morals / 2)
```

杀人时将敌人 ID 去重写入 `killList`。

### 掉落

- 胜利总是获得敌人 `gold`。
- 掉落候选依次为敌人装备武器、`item1`、`item2`、`item3`、`item4`。
- 负物品 ID 取绝对值。
- 物品 `1:19` 不进入普通背包，而是将敌人 ID 去重记录到 `stoneList`。
- 坛地图物品 `21–28` 只有在 `actor.tanId + 20 === itemId` 时允许取得。
- 浏览器版已解除原作背包 20 种条目与已学武功 20 门限制；行囊和武功使用对象映射并允许滚动，正常掉落、任务奖励、交换、学习和修改器都不得再按条目数量拒绝写入。单项物品的数量上限仍按对应系统规则执行。

### 特殊战斗

- 悬赏敌人使用 ID `198`，但战斗属性来自任务系统生成的动态覆盖记录。
- 铸剑挑战敌人 ID `149`，连续四轮要求武器 ID `[8, 15, 25, 21]`，中途失败会清除临时指定武器并重置进度。
- 桃花源山大王 ID `162`，胜利后获得家园并初始化房间和家具状态。
- 坛敌人 ID 为 `163–170`，只有杀死当前坛敌人才推进奖励。

## 7. 人物、背包、修炼与生存

### 背包和装备

- 背包键采用 `kind:id`，其中 `1` 为物品、`2` 为武器、`3` 为防具。
- 武器为单槽。
- 防具按原始 `kind` 区分槽位，同类装备互斥。
- 装备属性通过 `derivedStats` 汇总，不要直接永久写入基础属性。
- 自制武器使用原作武器 31 号槽，并将前缀、中段、后缀和重铸次数纳入衍生属性。
- 角色整备界面按消耗品/秘籍、剑器/刀兵/杖棍/鞭索，以及衣装/内甲/饰品/鞋履/腰带/披风/工具分类；每件装备必须显示槽位、装备状态和攻击、防御、命中、闪避、四维等常驻加成摘要。

### 学习和修炼

- 向师父请教会消耗潜能并受师父、门派和等级要求限制。
- 秘籍使用原始技能表，并检查读书识字、自创门派和特定性别流程。
- 打坐速度、双倍内力门槛、内力上限增长、法力上限增长都保持原作整数公式。
- 加力不得超过有效内功等级的一半。
- 吸气按缺失气血和内功等级计算内力消耗。
- 疗伤恢复伤势上限并固定消耗 50 内力。
- 练功按基础功夫速度、经验、潜能和内力门槛成长。
- R 修炼菜单列出所有已经学会的拳脚、兵刃与轻功类专门功夫，不再要求先在功夫菜单中运用；是否已运用只作为菜单状态提示。练功仍严格检查伤势、兵器、基本功夫、实战经验和内力门槛，失败原因必须显示在修炼窗口内。
- 修炼和连续请教以原作 120 Hz 的 phase 频率运行。
- 内力菜单四项都必须先装备内功，法力菜单两项都必须先装备法术；条件不满足时不得显示“修炼中”。
- 打坐或冥思达到当前功夫所允许的内力/法力上限时必须自动停止，并在修炼窗口明确提示封顶原因。
- 行囊内使用、装备或卸下任何条目前必须先显示确认框，鼠标点击和键盘确认走同一路径。

### 生存

- 每 15 秒执行一次消化循环。
- 有食物和饮水时按原作恢复，并分别消耗一点。
- 没有食物时不恢复，但食物和饮水仍分别扣至零。
- 累计 12 小时增长一岁，并保留不足 12 小时的余数。
- 井水每次增加 20 饮水，不超过上限。
- 女儿红消耗一坛并推进 3 小时游戏时间。
- 任务期限使用同一个持久化游戏时钟，不使用独立、不可保存的真实时间计时器。

## 8. 任务、小游戏、铸剑与家园

### 任务

已实现任务族：

- 拜访、寻物、杀人三类主任务。
- 顾炎武奖励和过期处理。
- 捕快悬赏：五分钟冷却、十次轮转、动态敌人数值与奖励。
- 义工任务：原作随机的扫地、挑水、劈柴，体力消耗、四句过程文本、完成语和奖励。
- 石料任务：冷却、物品和动态奖励。
- 十种隐藏交换任务。
- 九坛流程和各阶段奖励。

只认击杀的进度必须在玩家选择“砍头”后更新；手下留情不能推进。

### 小游戏

- 钓鱼：检查钓竿、体力、鱼篓容量和鲜鱼奖励。
- 跳舞：120 Hz 方向节拍、倒计时和计分，高分写入存档。
- 投球：移动瞄准、投掷飞行、七次失败结束，高分写入存档。

### 铸剑

- 通过四轮武器匹配挑战后开放铸剑谷。
- 可选择四类自制武器。
- 初铸写入原作 31 号自制武器槽。
- 重铸费用为经验的一半，并增加重铸次数。
- 自定义名称、类型、前中后缀和次数都必须保存到 JSON。

### 家园

- 击败山大王后获得桃花源家园。
- 房间共三级。
- 升级费用和家具容量保持原作规则；一级房只能放一件家具。
- 支持五类家具、清空家具和房间入口分支。

## 9. 存档格式

完整世界存档的格式标识为：

```json
{
  "format": "rmxp-hero-original-world-save"
}
```

存档包含：

- `position`：地图 ID、坐标和面向。
- `actor`：基础属性、当前气血/内力/法力、年龄、食水、经验、潜能、道德、门派、师父、性别和头像。
- `inventory`、`weaponId`、`armorIds`。
- `skills`、`skillUse`、加力和法点。
- `killList`、`stoneList`、坛进度。
- 任务状态和持久化时钟。
- 铸剑挑战、自制武器与重铸字段。
- 家园、房间和家具字段。
- 跳舞和投球最高分。

导入时必须：

1. 验证 `format`。
2. 验证地图 ID 存在。
3. 通过 `normalize` 为旧存档补齐新增字段。
4. 不删除玩家手工添加的合法数值字段。

SSR / hydration 约束：

- `useState` 的首屏状态必须始终使用确定性的 `fresh()`，服务器和浏览器第一次渲染不可直接读取 `localStorage`。
- `fresh().savedAt` 使用空字符串，不能在首屏调用 `new Date()`。
- 页面挂载后的 `useEffect` 使用零延时回调调用 `loadLocalSave()`，并通过 `sync` 同时更新 `stateRef` 与 React state；清理函数必须取消定时器。
- 保存或导出时才写入当前 ISO 时间。违反此顺序会使服务器显示新游戏地图、客户端显示本地存档地图，从而产生 hydration mismatch。

新增持久化系统时必须同步更新初始状态、`normalize`、类型定义和 JSON 往返测试或人工检查。

## 10. 视觉实现约定

- 原作位图不直接作为生产素材发布。
- 当前地图使用三层原始图块编号作为语义输入，再由 Canvas 程序化生成颜色和地表细节。
- 一层地表按图块编号生成草地、土路、石地、水纹、草簇和碎石等确定性细节。
- 二、三层图块压平为低对比地面轮廓，不再绘制会遮住玩家、NPC 或交互物的高墙和屋顶。
- 正式版世界移动只限制在地图边界，并保留 NPC、通缉犯等可交互单位的阻挡；原图块墙体不再形成不可见碰撞。地图传送仍必须通过原事件入口。
- 人物、事件标记、战斗人物和界面使用 Canvas / CSS 像素形状。
- Canvas 固定逻辑分辨率为 `640 × 480`，每格 `32 × 32`，CSS 使用 `image-rendering: pixelated` 放大。
- 桌面布局根据视窗高度放大，整体最大宽度为 1600–1740px，右侧状态栏为 282–310px；移动端仍回退为单列。
- 视觉修改不能改变地图坐标、通行数据或事件位置。
- 交互对象必须比背景有更高对比度；不能只依赖颜色，至少还要有轮廓、形状或标记。
- 所有通用选择菜单使用接近画布满幅的大面板，按条目数自动使用一至三栏；桌面端不得依赖滚动条才能看全条目。
- 行囊、状态、功夫使用正式版独占 CSS 类名，禁止复用早期页面的 `.status-grid` 或 `.skill-list`；顶部页签固定为独立 48px 行，内容变化不得改变页签位置或高度。
- 原版功夫称号共 50 阶，255 级显示“返璞归真”；人物综合武境也按攻击、轻功、招架的原版公式映射 50 阶，状态页同时显示当前阶数。出手轻重是独立的 6 阶劲道评价，不是功夫境界。
- 功夫面板按内功、拳脚、剑术、刀法、杖法、鞭法、棍法、术法、轻功、招架与知识分类，并以独立标签区分当前运用、用于招架和仅已习得。
- 优先使用程序化绘制，避免为简单图标调用 Image Gen。只有确实需要原创位图且程序化方案明显不足时才使用 Image Gen。
- Web 版原创素材统一放在 `public/game-assets/`，并由 `manifest.json` 记录图集布局；第三方素材还必须同步登记到 `ART_CREDITS.md`，不能把“免费下载”当作可再发布授权。
- 地图和人物位图加载失败时必须回退到程序化绘制，不能阻塞地图循环、移动、事件交互或存档恢复。
- 当前原创国风图集提供 8×8 环境素材和五套 4×4 人物/方向素材；人物源图列顺序固定为正面、画面朝右、画面朝左、背面，运行时方向 4 必须取画面朝左列、方向 6 必须取画面朝右列。地图逻辑格仍为 32×32。
- NPC 外观必须优先读取原始记录的 `gender`、`age`、`type`、姓名和描述，根据儿童/青年/中年/老年及官差、商贩、文士、工匠、匪徒、武者、僧尼、道士等身份选型；不要退回仅按姓名随机分配。
- 不能把 35–54 岁女性无条件归为中年劳动者。阿绣（原数据名“阿绣”，22岁）使用花间俏丽侍女形象，李青照（37岁）使用成熟词人掌门形象；花间、红莲、武当、雪山与冰火人物优先使用门派专属像素服装和立绘。关键人物以姓名表稳定映射，不能依赖哈希碰运气。
- 花间人物不能全用同一个粉衣模板。阿绣、李青照、柳如是、聂隐娘分别使用侍女、词人掌门、艳丽文士和蓝银刺客四套四方向像素外观；其余花间女性稳定分散到四套模板，并继续复用各自命名立绘。
- 环境图块不可再用 `id % N` 无条件猜测地表。常用地表 ID 必须明确映射，普通图块按原 RMXP 8 列图集的语义范围映射，使相邻的道路、屋顶、墙体、山林和院落仍形成连续多格结构。
- 地图建筑以入口事件为锚点生成连续多格立面，屋顶、墙、窗、门和门槛必须组成一个整体；不得把每个旧建筑图块替换成一栋完整小房子。室外山谷、岛屿、郊外等入口不能误画成商铺。
- 69 张地图通过 `mapTheme` 分为城镇、室内、山地、雪地、水域、坛场和秘境；`authoredRoads` 必须把传送入口连接到稳定中心节点，`drawAuthoredTerrain` 负责道路边界、室内围墙、主题色调和避开事件格的确定性植被。场景不再逐格显示旧 RMXP 高层图块。
- 第二代 `wuxia-map-modules-v2.png` 是当前地图生产图集，包含无缝草地、石路/土路边界、水岸、桥、连续屋顶/墙体、院墙、树石和交互物。第一代环境图集仅保留为历史素材，不再由运行时加载。
- `drawFactionLandmarks` 为玉女峰、红莲教、武当山、冰火岛、大雪山、茅山、长白山和九坛绘制大型五层门派建筑、庭院、山门、旗色和主题装饰；这些地图不能只剩主题地表和零散植物。
- 室内家具使用独立 `wuxia-indoor-furniture-v1.png` 图集。`indoorFurniture` 按住宅/客房、药店、裁缝店、杂货/豆腐店、兵器行/武馆、客栈、衙门/大厅和通用室内生成不同布局，并必须避开事件格。
- 人物立绘统一通过 `CharacterPortrait` 复用 5×4 图集，普通交谈、NPC 查看、自由对话、玩家状态和战斗必须使用同一人物分类结果。成年女性可采用唯美国风与更鲜明的身材设计；儿童必须保持年龄适宜和非性化。

## 11. 测试与完成标准

标准验证命令：

```bash
npm run lint
npm test
```

`npm test` 会：

1. 执行生产构建。
2. 使用 `tsx --test` 运行 TypeScript 游戏逻辑测试。
3. 运行服务器渲染测试。

当前基线：

- 105 项游戏逻辑测试通过。
- 3 项服务器渲染测试通过。
- ESLint 通过。
- 生产构建通过。

测试文件职责：

- `event-coverage.test.ts`：69 张地图所用指令码、条件和传送参数覆盖。
- `combat-parity.test.ts`：基础战斗整数公式。
- `original-battle.test.ts`：完整战斗确定性和剧情逃跑规则。
- `special-system.test.ts`：绝招需求、消耗、冷却和状态。
- `battle-settlement.test.ts`：金钱、掉落、杀人和道德。
- `inventory-system.test.ts`：物品、战斗物品、装备和属性。
- `npc-system.test.ts`：商店、拜师和学习。
- `npc-lore.test.ts`：全 NPC 档案覆盖与世界/人物/玩家/任务提示词。
- `lm-studio.test.ts`：本地模型和端点默认配置。
- `scene-event.test.ts`：场景事件、钓鱼、井水、酒、Boss、坛和家园入口。
- `task-system.test.ts`：全部任务族和奖励。
- `cultivation-system.test.ts`：修炼公式。
- `survival-system.test.ts`：消化、恢复和年龄。
- `life-system.test.ts`：铸剑、自制武器和房屋。
- `skill-system.test.ts`：武功运用和招架。
- `cheat-system.test.ts`：秘技快捷强化、数值边界和功夫等级上限。
- `keyboard-mapping.test.ts`：互动确认键与禁用 F 系列快捷键的静态回归。
- `status-system.test.ts`：原版武功等级称号、人物门派、外貌与综合评价。
- `rendered-html.test.mjs`：入口页和 `/original` 服务器渲染。

任何数值逻辑修改都应增加或修改对应单元测试。仅修改文档时无需重新构建，但提交前必须运行 `git diff --check`。

## 12. Sites 构建与发布

仓库包含 `.openai/hosting.json`，因此网站改动完成后必须遵循 Sites skill。

发布原则：

1. 先通过完整构建和测试。
2. 提交精确源代码状态。
3. 将该提交推送到 Sites 源仓库 `main`。
4. 使用 Sites 插件的 `scripts/package-site.sh` 打包相同提交生成的 `dist/`。
5. 用准确的 commit SHA 保存新版本。
6. 优先使用 owner-only private deployment。
7. 轮询到 `succeeded` 后再报告完成。
8. 使用返回的生产 URL 在 Codex 中打开。

不要把短期仓库凭据写入 Git remote、文件、日志或文档。`.openai/hosting.json` 只保留 project ID 以及可选 D1/R2 逻辑绑定。

若用户只明确要求本地文档或 Git 提交，不要擅自部署；如果改动会影响实际网站行为，则默认构建并发布。

## 13. Git 与工程卫生

- 工作树可能包含用户改动；修改前先运行 `git status --short`。
- 不要覆盖与当前任务无关的文件。
- 使用 `apply_patch` 修改手写文件。
- `npm` 或 `npx` 偶尔会机械改写 `package-lock.json` 中可选平台包的 `libc` 字段；如果依赖没有变化，不要提交这种噪声，恢复为 HEAD 内容。
- 不提交临时归档、凭据、浏览器文件或 `/tmp` 内容。
- 提交前执行 `git diff --check` 并确认 `git status --short` 只包含预期文件。
- 提交信息使用简洁的英文祈使句，描述实际结果。

## 14. 已知边界与修改风险

- `original-world.tsx` 是目前最大的编排文件。重构时要保持输入优先级：对话框和模态菜单必须先于世界移动处理按键。
- 地图事件页条件只实现原作地图实际用到的 Ruby 子集；遇到新条件时应增加安全解析器和测试，不要执行任意 Ruby 或 JavaScript。
- 事件脚本型 NPC 的显示、碰撞、交互共享 `eventScene` 判定。修改其中一个行为时必须检查另外两个。
- 地图绘制每帧运行，避免在 `draw` 中加入网络请求、React 状态更新或大规模对象分配。
- 战斗中的 `enemyOverride` 对悬赏动态敌人至关重要；读取敌方属性时优先使用 override，再回退原数据库。
- 普通切磋、生死战和剧情战不能混用结算流程。
- 战斗小说化必须以原战斗引擎日志和结算前后气血为不可修改的事实；LM Studio 只负责流式演绎，不能反向决定命中、伤害、伤势或胜负。当前回合演绎完成前禁止进入下一回合，避免异步战报乱序。
- 师父的实战四维门槛必须读取 `derivedStats` 实时派生值，与原版 RGSS 属性 getter 和状态栏保持一致；不要读取存档中的 `actor.bon/int/agi/str` 缓存。
- `endSpar` 也承担战后人物状态收束；修改失败或退出逻辑时要检查它是否仍适用于对应模式。
- 生产域在某些自动化出口可能被 Cloudflare 安全层返回 403；Sites deployment 的 `succeeded`、本地生产构建和服务器渲染测试是发布验证基础，最终链接由已登录用户打开。

## 15. 最近完成的工作

按时间倒序：

- 扩充花间派像素多样性与室内陈设：阿绣、李青照、柳如是、聂隐娘获得匹配立绘的独立四方向外观；新增 64 格家具图集，按住宅、商店、武馆、客栈、衙门等类型布置床榻、桌椅、柜架、屏风、柜台、兵器与生活器具。
- 完成人物一致性与门派气派化：修正阿绣、李青照等女性年龄误判，加入关键人物稳定立绘索引和花间/红莲/武当/雪山/冰火专属四方向服装；为主要门派与九坛增加大型门派建筑、庭院、山门、旗色和主题地标。
- 重写 69 图场景生成：第二代无缝模块图集、七类地图主题、入口路网、道路边界、室内围墙、确定性植被与多格建筑共同替代旧图块贴纸；所有立绘容器固定正方形比例避免拉伸，自由对话输入区扩为底部整行。
- 全面升级地图和人物表现：交互感叹号移至头顶上方；商业街等城镇地图按入口事件生成连续多格建筑；新增 20 人国风立绘图集并接入普通交谈、自由对话、人物状态和战斗；自由对话重构为左侧立绘、上方记录、底部单行行动/语言交互区。
- 修正玩家左右朝向并扩展 NPC 美术到五套四方向图集：按男女老少及官差、商贩、文士、工匠、武者、匪徒、僧尼和道士等身份稳定匹配；地图绘制从 ID 取模改为原图块语义范围映射，恢复道路、山水、房屋与多格院落的连续结构。
- 接入第一版原创国风像素素材系统：8×8 环境图集替换程序化地表与高层轮廓，4×4 人物图集提供男女主角及 NPC 四方向外观；保留素材加载失败时的程序化回退，并新增素材 manifest 与授权来源台账。
- 新增 LM Studio 武侠小说战报：每回合把双方背景、外貌、师承、武境、装备、原始招式、实际伤害与结算前后气血交给模型流式扩写；最新正文高亮，完整战斗历史保留，并按伤害比例严格约束伤势程度。
- 重构 NPC 自由对话为临场互动：地图内双人气泡、玩家行动/语言双输入、NPC 状态/动作/语言三段流式反馈；当前相遇按上下文预算保留历史，结束或移动立即清空，不再跨会话持久化。
- 解除原作背包 20 种和武功 20 门限制：正常掉落、石料/九坛/隐藏交换奖励及完整修改器都可继续写入；行囊和武功面板依靠滚动承载全部条目，保留单项数量上限与装备槽规则。
- 将秘技升级为六分类完整修改器：30 项人物数值支持步进、直接输入、范围提示与统一钳制；支持全部物品装备增删、全部武功习得/移除/定级、姓名性别/门派师父切换、家园铸剑与任务状态修改，并可逐个复活已击杀 NPC。移除装备或武功时同步清理装备槽和运用槽，避免坏档。

- 接入本地 LM Studio NPC 自由对话：指定 Qwen 模型、SSE 流式回复、短上下文与关闭思考优化；为全部 198 条有效人物记录生成结合原作描述、地图、身份、武功和数值的稳定档案，并同步展示于 NPC 状态与系统提示词。

- 补全三角石板反馈与展示：六位原版掌门首次掉落时战果明确列出石板，`stoneList` 在行囊合并显示为关键物品及收集进度，同一掌门仍不可重复掉落。
- 修复白瑞德等师父误判实战四维的问题：拜师门槛现在实时计入基本功与装备加成，与状态栏及原版属性 getter 一致；白瑞德仍要求内力上限 1200 或指定武功 100 级，并要求实战根骨 32。
- 将角色整备界面升级为现代武侠游戏式门类布局：行囊按物品、武器类型与防具槽位分组并显示属性摘要；武学按十一类功夫分组，清晰标记运用、招架和习得状态。
- 重构桌面行囊/状态/功夫面板，固定顶部页签并隔离旧版 CSS 冲突；状态卡片分栏展示，功夫和综合武境显示 50 阶天梯，满级为返璞归真；补全 NPC 先天四维、加力法点、外貌福缘和掉落率。
- 统一放大全部选择菜单并按数量自动排成一至三栏，移除桌面滚动条；NPC 查看恢复原作描述、装备、携带物、武功与完整战斗资料；打坐/冥思达到修为上限后自动停止并提示。
- 放大请教与修炼菜单为桌面双栏全屏面板，武功条目一屏可读；R 修炼可选择全部已学可练专门功夫，连续练功提供实时进度，并在面板内直接说明伤势、兵器、基本功、经验或内力等失败原因。
- 修复请教菜单与连续修炼反馈：自动请教/修炼中仍可用 W/S 停止并换项，鼠标与键盘统一启动流程；请教、打坐、冥思和练功显示实时进度条、当前等级及资源变化。侧栏银两/经验/潜能改为紧凑大数格式并保留完整悬停值。
- 修复原版砍头闭环：普通 NPC 记录击杀、扣道德并在地图留下不可交互遗骸，173–194 号喽啰消失；通缉犯累计追杀数，杀手任务累计完成数，坛主仅按坛规则推进；恢复临终台词，并避免任务奖励覆盖战利品提示。
- 扩展文字状态系统：外部栏实时展示五项生存条、四维、战斗数值和资源；人物状态页展示门派、师承、外貌、装备、履历及原版综合武艺评价；每门武功恢复“登堂入室”等原版五级称号。秘技标注理论上限，数值与功夫均支持逐项 MAX，并增加全参数 MAX。
- 修复人物创建中文输入法：输入框与 IME composing 事件优先于全局键盘处理；修复战斗结束后按钮及 `E/Enter` 确认无法进入战果处理的问题。
- 统一 Mac 友好键位：取消字母 F、F1–F12、Z 和空格操作，互动及菜单确认统一为 `E/Enter`；同步全部界面提示。
- 新增正式秘技菜单：主菜单与侧栏入口，`K` 快捷键，资源/属性/功夫三类修改，一键宗师二次确认，并保留 JSON 备份和手动保存工作流。
- 修复内部传送事件名泄漏：`EVxxx` 门名自动回退到目标地图名称，红莲教出口显示“五指山”。
- 修复本地存档导致的 hydration mismatch：首屏使用确定性新游戏状态，挂载后异步恢复 `localStorage`，保存与导出时才生成时间；增加回归测试。
- 正式版界面与地图视觉重构：根路由直接进入完整游戏；移除入口选择；新增青色交互物、绿色房门/地图入口和文字标签；高层图块压平以消除墙体遮挡；增加丰富地表纹理、轻量暗角和桌面大屏布局。提交 `1aba988`，Sites v27。
- 修复脚本型 NPC 的交互与碰撞：玩家不能再穿过 NPC；确认键会在当前格、面前和相邻格优先寻找人物；兼容旧存档重叠状态。提交 `0466ff5`，Sites v26。
- 让所有脚本型 NPC 可见：程序化人物、亮色轮廓、脚下交互环、头顶提示、近距离姓名和红色通缉犯标记。提交 `2076b3e`，Sites v25。
- 完成生死战闭环：战斗物品、原作逃跑公式、掉落、金钱、砍头/留情、杀人记录、道德公式、悬赏和坛进度。提交 `0616a1a`。
- 完成最终生产审计并发布完整单机版。提交 `96c20b7`，Sites v24。
- 更早阶段完成全部任务族、隐藏交换、九坛、小游戏、铸剑四轮挑战、自制武器、重铸、桃花源家园、房间与家具。

后续每次完成重要功能或修复后，更新本节、相关规则和测试基线，避免本文件成为过期说明。
